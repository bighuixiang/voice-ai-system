export function roleTitle(_roleId: string, fallback: string): string {
  return fallback;
}

export function rolePrompt(_roleId: string, fallback: string): string {
  return fallback;
}

export function promptTitle(_promptId: string, fallback: string): string {
  return fallback;
}

export function promptText(_promptId: string, fallback: string): string {
  return fallback;
}

export function skillDescription(_skillId: string, fallback: string): string {
  return fallback;
}
