import crypto from "node:crypto";
export interface TaskPatternRetrievalInput { projectSlug: string; sceneFunction: string; targetGap: string; genreProfile: string; authorPreferences: string[]; sourceEligibility: "default_allowed" | "analysis_only" | "blocked"; applicablePatternIds: string[]; sourceFamilies: string[]; evidenceSnapshots: Array<{ patternId: string; snapshotId: string; reason: string }>; budget: { maxPatterns: number; timeWindowMs: number }; sourceRefs: string[]; }
export interface TaskPatternRetrievalManifest { schemaVersion: "task-pattern-retrieval.v1"; projectSlug: string; queries: string[]; selectedPatternIds: string[]; excluded: Array<{ patternId: string; reason: string }>; truncatedPatternIds: string[]; sourceFamilies: string[]; evidenceSnapshots: Array<{ patternId: string; snapshotId: string; reason: string }>; budget: { maxPatterns: number; timeWindowMs: number }; sourceRefs: string[]; fingerprint: string; }
function hash(value: unknown) { return crypto.createHash("sha256").update(JSON.stringify(value)).digest("hex"); }
export function buildTaskPatternRetrieval(input: TaskPatternRetrievalInput): TaskPatternRetrievalManifest { if (!input.projectSlug.trim() || !input.sceneFunction.trim() || !input.targetGap.trim() || !input.genreProfile.trim()) throw new Error("RETRIEVAL_TASK_FIELDS_REQUIRED"); if (input.budget.maxPatterns < 1 || input.budget.timeWindowMs < 1) throw new Error("RETRIEVAL_BUDGET_REQUIRED"); if (!input.sourceRefs.length) throw new Error("RETRIEVAL_EVIDENCE_REQUIRED"); const query = `${input.sceneFunction}|${input.targetGap}|${input.genreProfile}`; const excluded: Array<{ patternId: string; reason: string }> = []; if (input.sourceEligibility !== "default_allowed") for (const id of input.applicablePatternIds) excluded.push({ patternId: id, reason: input.sourceEligibility === "analysis_only" ? "SOURCE_ANALYSIS_ONLY" : "SOURCE_BLOCKED" }); const eligible = input.sourceEligibility === "default_allowed" ? input.applicablePatternIds : []; const selectedPatternIds = eligible.slice(0, input.budget.maxPatterns); const truncatedPatternIds = eligible.slice(input.budget.maxPatterns); const snapshots = input.evidenceSnapshots.filter((snapshot) => selectedPatternIds.includes(snapshot.patternId)); const base = { schemaVersion: "task-pattern-retrieval.v1" as const, projectSlug: input.projectSlug, queries: [query], selectedPatternIds, excluded, truncatedPatternIds, sourceFamilies: [...input.sourceFamilies], evidenceSnapshots: snapshots, budget: input.budget, sourceRefs: [...input.sourceRefs] }; return { ...base, fingerprint: hash(base) }; }
