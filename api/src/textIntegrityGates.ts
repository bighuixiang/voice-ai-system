export function diagnoseImport(input: { encodingValid: boolean; rawHash: string; newlineKinds: readonly string[]; semanticHash: string }): { blocked: boolean; rawHash: string; mechanicalCandidate: boolean; semanticHash: string } { return { blocked: !input.encodingValid, rawHash: input.rawHash, mechanicalCandidate: input.encodingValid && input.newlineKinds.length > 1, semanticHash: input.semanticHash }; }
export function gatePunctuationRepair(input: { profileAllows: boolean; protectedBlock: boolean; semanticEquivalent: boolean; globalReplace: boolean }): { applicable: boolean; blocked: boolean } { return { applicable: input.profileAllows && !input.protectedBlock, blocked: input.globalReplace || !input.semanticEquivalent }; }
export function isolateModelPackaging(input: { uniqueBodyBoundary: boolean; wrapperFields: readonly string[]; rawPreserved: boolean }): { canAdopt: boolean; isolated: string[]; rawPreserved: boolean } { return { canAdopt: input.uniqueBodyBoundary && input.rawPreserved, isolated: [...input.wrapperFields], rawPreserved: input.rawPreserved }; }
export function settleRoundTrip(input: { firstStructure: string; secondStructure: string; mechanicalRepairs: number; ambiguousAnchors: number; oldBaseline: boolean }): { equivalent: boolean; staleAnchors: boolean; accepted: boolean } { const equivalent = input.firstStructure === input.secondStructure && input.mechanicalRepairs === 0; return { equivalent, staleAnchors: input.ambiguousAnchors > 0, accepted: equivalent && !input.oldBaseline }; }
