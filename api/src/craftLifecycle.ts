import crypto from "node:crypto";
export type CraftLifecycle = "candidate" | "approved" | "probation" | "validated" | "rejected" | "retired";
export interface CraftLifecyclePattern { schemaVersion: "craft-lifecycle.v1"; patternId: string; projectSlug: string; lifecycle: CraftLifecycle; sourceRefs: string[]; history: Array<{ from: CraftLifecycle; to: CraftLifecycle; actor: string; reason: string; experimentId: string | null; at: string }>; fingerprint: string; }
function hash(value: unknown) { return crypto.createHash("sha256").update(JSON.stringify(value)).digest("hex"); }
export function createCraftPattern(input: { patternId: string; projectSlug: string; lifecycle: CraftLifecycle; sourceRefs: readonly string[] }): CraftLifecyclePattern { if (!input.sourceRefs.length) throw new Error("CRAFT_LIFECYCLE_SOURCE_REQUIRED"); const base = { schemaVersion: "craft-lifecycle.v1" as const, patternId: input.patternId, projectSlug: input.projectSlug, lifecycle: input.lifecycle, sourceRefs: [...input.sourceRefs], history: [] }; return { ...base, fingerprint: hash(base) }; }
export function transitionCraftPatternLifecycle(pattern: CraftLifecyclePattern, input: { target: CraftLifecycle; actor: string; reason: string; experimentId: string | null }): CraftLifecyclePattern { if (!input.actor.trim() || !input.reason.trim()) throw new Error("CRAFT_LIFECYCLE_AUTH_REQUIRED"); const transitions: Record<CraftLifecycle, CraftLifecycle[]> = { candidate: ["approved", "rejected", "retired"], approved: ["probation", "rejected", "retired"], probation: ["validated", "rejected", "retired"], validated: ["rejected", "retired"], rejected: ["retired"], retired: [] }; if (!transitions[pattern.lifecycle].includes(input.target)) throw new Error("CRAFT_LIFECYCLE_TRANSITION_INVALID"); if (input.target === "probation" && !input.experimentId) throw new Error("CRAFT_EXPERIMENT_REQUIRED"); if (input.target === "validated" && !input.experimentId) throw new Error("CRAFT_VALIDATION_EXPERIMENT_REQUIRED"); const history = [...pattern.history, { from: pattern.lifecycle, to: input.target, actor: input.actor, reason: input.reason, experimentId: input.experimentId, at: new Date().toISOString() }]; const base = { ...pattern, lifecycle: input.target, history }; return { ...base, fingerprint: hash({ ...base, fingerprint: undefined }) }; }
