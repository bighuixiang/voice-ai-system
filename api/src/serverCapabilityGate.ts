export function enforceServerCapability(input: { enabledCapabilities: readonly string[]; requiredCapability: string; migrationPreviewRef?: string }): { status: "allowed" | "capability_not_activated"; migrationPreviewRef?: string; writes: false } { const allowed = input.enabledCapabilities.includes(input.requiredCapability); return allowed ? { status: "allowed", writes: false } : { status: "capability_not_activated", migrationPreviewRef: input.migrationPreviewRef, writes: false }; }
