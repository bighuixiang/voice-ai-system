import crypto from "node:crypto";
export type ProseBlockMode = "scene" | "exposition" | "description";
export interface ProseSpecificityBlock { blockId: string; text: string; mode: ProseBlockMode; linkedFunction: "perception" | "conflict" | "choice" | "consequence" | null; hasSensoryEvidence: boolean; changesAction: boolean; }
export interface ProseSpecificityInput { sceneId: string; blocks: ProseSpecificityBlock[]; sourceRefs: string[]; }
export interface ProseSpecificityReport { schemaVersion: "prose-specificity-report.v1"; sceneId: string; status: "passed" | "blocked"; issues: string[]; blockFindings: Array<{ blockId: string; issues: string[] }>; fingerprint: string; }
function hash(value: unknown) { return crypto.createHash("sha256").update(JSON.stringify(value)).digest("hex"); }
export function evaluateProseSpecificity(input: ProseSpecificityInput): ProseSpecificityReport { const issues: string[] = []; const blockFindings: ProseSpecificityReport["blockFindings"] = []; let expositionRun = 0; for (const block of input.blocks) { const found: string[] = []; expositionRun = block.mode === "exposition" ? expositionRun + 1 : 0; if (expositionRun >= 2 || (block.mode === "exposition" && !block.linkedFunction)) found.push("CONTINUOUS_EXPOSITION"); if (block.mode === "exposition" && !block.linkedFunction) found.push("SETTING_DUMP"); if (/(宏大|永恒|伟大|终极|命运|无尽)/.test(block.text) && !block.changesAction) found.push("GRANDIOSE_ABSTRACTION"); const sensoryWords = (block.text.match(/[,，、]/g) || []).length; if (block.mode === "description" && sensoryWords >= 5 && !block.changesAction) found.push("SENSORY_LIST_STACK"); if (!block.linkedFunction && !block.changesAction) found.push("UNLINKED_ABSTRACTION"); if (found.length) { blockFindings.push({ blockId: block.blockId, issues: found }); for (const issue of found) if (!issues.includes(issue)) issues.push(issue); } } if (!input.sourceRefs.length) issues.push("SPECIFICITY_EVIDENCE_REQUIRED"); if (input.blocks.some((block) => !block.hasSensoryEvidence && !block.changesAction && !block.linkedFunction)) issues.push("CONCRETE_EVIDENCE_REQUIRED"); const base = { schemaVersion: "prose-specificity-report.v1" as const, sceneId: input.sceneId, status: issues.length ? "blocked" as const : "passed" as const, issues, blockFindings }; return { ...base, fingerprint: hash(base) }; }
